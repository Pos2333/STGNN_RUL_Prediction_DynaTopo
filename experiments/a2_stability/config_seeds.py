# ============================================================
# experiments/a2_stability/config_seeds.py
# 多 seed 稳定性实验配置
# ============================================================
# 实验目的：
#   在修正 A2（attention_generator 加入 softmax 归一化）之后，
#   通过多随机种子重复训练，评估各模型（尤其 A2）指标的离散程度，
#   判断 A2 与 A1 之间是否存在显著、稳定的性能差异。
#
# ⚠️ 本实验与主线（seed=42）完全隔离：
#   - 模型输出到 experiments/models/
#   - 日志输出到 experiments/logs/
#   - 不修改主线代码与主线结果
# ============================================================

# ---- 随机种子列表 ----
# 内部探索用，任意整数均可。
# ⚠️ 若需写入论文，建议改用连续种子（如 [42, 43, 44, 45, 46]），
#    便于读者理解"这是 N 次重复实验"。
SEEDS = [114514, 99, 233, 7, 2024]

# ---- 参与实验的模型 ----
#   static : 静态基线（STGNN_Static，原论文架构）
#   A1B1 ~ A2B2 : 2×2 消融矩阵全部四组
PRESETS = ['static', 'A1B1', 'A1B2', 'A2B1', 'A2B2']

# ---- 训练轮数与早停（沿用主线 config.py 的值）----
NUM_EPOCHS = 200
EARLY_STOP_PATIENCE = 20

# ---- 输出目录（相对项目根目录）----
MODEL_DIR = 'experiments/models'
LOG_DIR = 'experiments/logs'
